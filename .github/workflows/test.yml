name: Test

on:
  workflow_dispatch:
  push:
    branches:
      - '*'

jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
        with:
          version: 12

      - name: install
        run: pnpm install
      - name: typecheck
        run: pnpm build
      - name: transpile
        run: pnpm build
      - name: test
        run: pnpm test
